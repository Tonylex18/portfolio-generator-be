import { MonnifyConfig, MonnifyResponse, PaymentDetails } from "./types";
export default class Monnify {
    private publicKey;
    private contractCode;
    private mode;
    constructor({ publicKey, contractCode, mode }: MonnifyConfig);
    private loadMonnifyScript;
    initializePayment(paymentDetails: PaymentDetails): Promise<MonnifyResponse>;
}
